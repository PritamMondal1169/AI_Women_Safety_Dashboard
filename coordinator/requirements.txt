# SafeSphere Coordinator — Python Dependencies
# Install with: pip install -r coordinator/requirements.txt

# API framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Process Manager
gunicorn>=22.0.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Push notifications (optional — mock mode if not installed)
# firebase-admin>=6.5.0

# External APIs
httpx>=0.27.0
twilio>=9.0.0
africastalking>=1.3.0

# WebSocket support (included in uvicorn[standard])
websockets>=12.0

# Form data support (required for FastAPI OAuth2)
python-multipart>=0.0.9

# Already in main requirements.txt
python-dotenv>=1.0.0
requests>=2.31.0
